import { BaseStepComponentProps } from "@/components/analysis/definitions/base";

const TableDataVisualizer: React.FC<BaseStepComponentProps> = ({ stepResult, analysisId, analysisType, step }) => {
    return <div>TableDataVisualizer</div>;
};

export default TableDataVisualizer;
