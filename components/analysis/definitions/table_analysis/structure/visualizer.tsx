import { BaseStepComponentProps } from "@/components/analysis/definitions/base";

const TableStructureVisualizer: React.FC<BaseStepComponentProps> = ({ stepResult, analysisId, analysisType, step }) => {
    return <div>TableStructureVisualizer</div>;
};

export default TableStructureVisualizer;
